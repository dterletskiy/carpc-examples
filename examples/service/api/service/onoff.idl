author: dterletskiy;
interface: onoff;
version: 2.0.0;

import: "api/service/navigation/base.ddl";

ipc: true;

method: trigger_state( string state, size_t timeout ) -> ( bool result );

method: start( );

attribute: string current_state;
